services:
  mysql:
    image: mysql:8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123
    ports:
      - "3307:3306"
    volumes:
      - ./mysql:/var/lib/mysql
      - ./init.sql:/code-executor-entrypoint-initdb.d/init.sql